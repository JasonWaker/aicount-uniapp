# AI倒数日 uni-app 迁移版

由微信小程序项目自动迁移生成，原项目文件未改动。

## 使用

1. 使用 HBuilderX 打开本目录。
2. 运行到微信开发者工具。
3. 资源已迁移到 `static/assets`，页面继续引用项目内 SVG 图标和模板图片。

## 迁移说明

- 小程序 `.wxml/.wxss/.js` 已合并为对应的 `.vue` 页面。
- 微信 API 已尽量替换为 `uni.*`，微信云能力保留在 MP-WEIXIN 环境。
- 页面路由、缓存 key、点击跳转和主要交互逻辑保持原项目一致。
